<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.damoa.repository.interfaces.FreelancerRepository">

    <insert id="insertFreelancer">
        INSERT INTO freelancer_info(user_id, job_part, working_style, expected_salary, salary_type, experience, career, skill, portfolio, detail, link, created_at)
        VALUES(#{userId}, #{jobPart}, #{workingStyle}, #{expectedSalary}, #{salaryType}, #{experience}, #{career}, #{skill}, #{portfolio}, #{detail}, #{link}, now())
    </insert>

</mapper>