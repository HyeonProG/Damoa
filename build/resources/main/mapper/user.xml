<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper 
    namespace="com.damoa.repository.interfaces.UserRepository">

    <insert id="insertUser">
<<<<<<< HEAD
        INSERT INTO user(email, username, password, phone_number, social_type, user_type, created_at)
        VALUES(#{email}, #{username}, #{password}, #{phoneNumber}, #{socialType}, #{userType}, now())
=======
        INSERT INTO user(email, username, password, social_type, user_type, created_at)
        VALUES(#{email}, #{username}, #{password}, #{socialType}, #{userType}, now())
>>>>>>> dev_kgr
    </insert>

    <select id="checkDuplicateEmail" resultType="int">
        SELECT count(*) FROM user WHERE email = #{email}
    </select>

    <select id="findDuplicatePhoneNumber" resultType="int">
        SELECT count(*) FROM user WHERE phone_number = #{phoneNumber}
    </select>

    <select id="findByEmail">
        SELECT * FROM user WHERE email = #{email}
    </select>

</mapper>