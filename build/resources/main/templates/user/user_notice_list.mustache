{{>layout/header}}
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

<style type="text/css">
    .notice-title {
        text-align: center;
        margin: 20px 0;
    }
    .notice-list-container {
        width: 80%;
        max-height: 60vh;
        border-radius: 10px;
        box-shadow: 2px 2px 5px #dee2e6;
        padding: 20px;
        margin: auto; /* Center the container */
    }
    .notice-box {
        border: 1px solid #dee2e6;
        border-radius: 10px;
        background: #f8f9fa;
        transition: background 0.3s;
    }
    .notice-box:hover {
        background: #e9ecef;
    }
    .pagination {
        justify-content: center;
        margin: 20px 0;
    }
</style>

<div class="notice-title">
    <h3>다모아의 새로운 소식들</h3>
    <h5>신규 기능, 이벤트, 점검 일정 등 다양한 이야기를 확인해주세요.</h5>
</div>

<div class="notice-list-container">
    <table class="table table-bordered notice-table">
        <thead>
        <tr>
            <th>제목</th>
            <th>작성일</th>
        </tr>
        </thead>
        <tbody>
        {{#noticeList}}
            <tr class="notice-box" onclick="location.href='/notice/detail/{{id}}'">
                <td>{{title}}</td>
                <td>{{createdAt}}</td>
            </tr>
        {{/noticeList}}
        </tbody>
    </table>
</div>

<div class="d-flex justify-content-center pagination">
    <button class="btn btn-secondary mx-2" onclick="location.href='/notice/list/{{lastPage}}'">이전 페이지</button>
    <button class="btn btn-primary mx-2" onclick="location.href='/notice/list/{{currentPageNum}}'">{{currentPageNum}}</button>
    <button class="btn btn-secondary mx-2" onclick="location.href='/notice/list/{{nextPage}}'">다음 페이지</button>
</div>

{{>layout/footer}}