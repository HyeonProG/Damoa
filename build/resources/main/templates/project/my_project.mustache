{{>layout/header}}
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
<style type="text/css">
    .project-container {
        height: 60vh;
        display: flex;
    }
    .nav-bar-project {
        width: 30%;
        height: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: space-evenly;
        background: #F3F3F3;
        padding: 10px; /* 여백 추가 */
    }
    .project-list {
        width: 70%;
        overflow-y: auto; /* 세로 스크롤 가능하게 */
    }
</style>
<div class="container-fluid">
    <div class="project-container">
        <div class="nav-bar-project">
            <h5>내 프로젝트 목록 보기</h5>
            <hr>
            <h5 style="color : #0091ea"><b>1. 게시 중인 프로젝트</b></h5>
            <h5>2. 매칭 중인 프로젝트</h5>
            <h5>3. 진행 중인 프로젝트</h5>
            <h5>4. 완료된 프로젝트</h5>
        </div>
        <div class="project-list">
            <h2>1. 게시 중인 프로젝트</h2>
            <table class="table">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>프로젝트명</th>
                </tr>
                </thead>
                <tbody>
                {{#projectListForPaging}}
                    <tr>
                        <td>{{id}}</td>
                        <td>{{projectName}}</td>
                    </tr>
                {{/projectListForPaging}}
                </tbody>
            </table>
        </div>
    </div>
</div>
{{>layout/footer}}
